{
  "name": "todo-list-app",
  "version": "1.0.0",
  "description": "To-Do Application using JSON Server",
  "scripts": {
    "server": "json-server --watch db.json --port 3000"
  },
  "devDependencies": {
    "json-server": "^0.17.4"
  }
}

